# 반복수학 문제 추출기

`반복수학_extractor.py`는 중학·고등 반복수학 및 반복수학 파워 PDF에서 문제를 PNG로 추출합니다.

## 실행

```powershell
.\.venv\Scripts\python.exe .\반복수학_extractor.py "C:\경로\반복수학.pdf"
```

출력 위치를 직접 지정하려면 다음처럼 실행합니다.

```powershell
.\.venv\Scripts\python.exe .\반복수학_extractor.py "C:\경로\반복수학.pdf" -o "output\반복수학"
```

## 적용 규칙

- 색상으로 구분된 큰 문제 번호를 문제 시작점으로 인식
- 큰 번호와 공통 발문, 그 아래 `(1)`, `(2)` 등 모든 소문항을 한 장으로 저장
- 개념 기본 문제도 동일하게 묶으며, 발문을 소문항마다 반복하지 않음
- 한 문제의 소문항이 왼쪽 단에서 오른쪽 단 상단으로 이어지면 읽는 순서대로 연결
- 20pt PPT 배치 높이를 넘는 긴 문제는 소문항 사이의 공백에서 자동 분할
- 분할된 두 번째 이미지부터도 큰 번호와 공통 발문을 상단에 반복
- 분할 파일은 `015p_001-01.png`, `015p_001-02.png`처럼 연속 저장되어 PPT의 다음 슬라이드로 이어짐
- 청록색 `TIP` 표식과 연노랑 설명 상자는 문제 이미지에서 제거
- `풍쌤 POINT` 제목과 연한 파란 설명 상자도 문제 이미지에서 제거
- 긴 문항을 나눈 모든 후속 이미지에 큰 번호와 공통 발문을 반복
- 후속 이미지의 본문은 발문 아래 동일한 간격에서 시작하도록 세로 위치 정규화
- `①~⑤` 선택지가 있으면 큰 색상 번호 단위로 한 문제로 저장
- 객관식과 주관식 모두 지원
- 그래프, 도형, 조건 박스, 풀이용 내부 박스 보존
- 실제 세로 단 구분선을 탐지하여 옆 문제 제외
- 숫자와 수식 획이 잘리지 않도록 좌우 여백 추가
- 파일명은 PDF 페이지 순서에 따라 `009p_001.png` 형식으로 저장 (책에 인쇄된 쪽수와 다를 수 있음)

기존 소문항별 결과와 섞이지 않도록 새 출력 폴더를 사용하세요. 웹사이트에서 이미
다운로드한 ZIP/PPT는 자동 변경되지 않으므로 PDF를 다시 추출해야 합니다.
